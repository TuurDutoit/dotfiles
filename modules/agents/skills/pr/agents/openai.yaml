interface:
  display_name: "PR"
  short_description: "Create a PR and monitor CI"
  default_prompt: "Use $pr to create a pull request and monitor CI until it is green."
policy:
  allow_implicit_invocation: true
